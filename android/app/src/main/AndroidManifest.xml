<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.lifemove">

    <application
        android:name=".utils.LifeMoveApplicationUtils"
        android:allowBackup="true"
        android:icon="@mipmap/lifemove_badge"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lifemove_badge_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.HomeActivity" />
        <activity
            android:name=".view.ResetPasswordActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".view.RegistrationActivity" />
        <activity
            android:name=".view.LoginActivity">
        </activity>
        <activity
            android:name=".view.SplashScreenActivity"
            android:theme="@style/AppCompatFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>